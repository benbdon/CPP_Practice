{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CPP compile",
            "type": "shell",
            "windows": {
                "command": "g++ *.cpp -g -o main.exe",
            },
            "linux": {
                "command": "g++ *.cpp -g -std=c++11 main"
            },
            "group": "build",
            "presentation": {
                // Reveal the output only if unrecognized errors occur.
                "reveal": "silent"
            },
        }
    ]
}